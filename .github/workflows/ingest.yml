name: Ingest

on:
  schedule:
    - cron: '0 3 */4 * *'
  workflow_dispatch: {}

jobs:
  run-ingest:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14'
      - run: pip install -r gp-ingest/requirements.txt
      - run: python gp-ingest/drive_ingest.py
        env:
          GDRIVE_REFRESH_TOKEN: ${{ secrets.GDRIVE_REFRESH_TOKEN }}
          GDRIVE_CLIENT_ID: ${{ secrets.GDRIVE_CLIENT_ID }}
          GDRIVE_CLIENT_SECRET: ${{ secrets.GDRIVE_CLIENT_SECRET }}
          GDRIVE_FOLDER_ID: ${{ secrets.GDRIVE_FOLDER_ID }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      - run: |
          git config --global user.email "bot@ingest.com"
          git config --global user.name "Ingest Bot"
          git add manifest.json gp-ingest/created_google_docs_index.json gp-ingest/gp_extracted_examples.json
          git commit -m "Auto-update manifest" || echo "Nothing to commit"
          git push
